import { MAPBOX_URL } from "../constants/API";

export class MapBox {
  constructor(inputs){
    this.inputs = inputs;
  }

  *get(inputs){
    // {tileset_id}/{zoom}/{x}/{y}.{format}

    const BaseURL = MAPBOX_URL;


  }
}